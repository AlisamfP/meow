import React, { PropTypes } from 'react'
import { Fixed } from 'react-layout-pane'

import './index.css'

const TopBar = () => {
  return (
    <Fixed className="TopBar">
      MEOW
    </Fixed>
  )
}

TopBar.propTypes = {}

export default TopBar
