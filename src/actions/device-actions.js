import * as types from '../constants/action-types'

// export function fetchDevice({deviceUuid}) {
//   return (dispatch) => {
//     dispatch({
//       type: types.FETCH_DEVICE_REQUEST,
//       deviceUuid
//     })
//     return _fetchDevice({deviceUuid})
//       .then((device) => dispatch(actionFetchDeviceSuccess(device)))
//       .catch((error)=> dispatch(actionFetchDeviceFailure(error)))
//   }
// }
//
// function actionUpdateDeviceFailure(error) {
//   return {
//     type: types.UPDATE_DEVICE_FAILURE,
//     error: error
//   }
// }
